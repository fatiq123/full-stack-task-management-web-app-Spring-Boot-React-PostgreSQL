package com.taskmanagement.app.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS configuration is now handled directly in WebSecurityConfig
}
